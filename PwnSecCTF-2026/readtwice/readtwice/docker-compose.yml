services:
  challenge:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3001:3000"
    environment:
      PORT: "3000"
      APP_URL: "http://localhost:3000"
      FLAG: "flag{browser_does_more_than_pixels}"
      BOT_TOKEN: "local-bot-token"
      SESSION_SECRET: "looosoosw"
